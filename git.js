// Git: version control system: to track versions/manage changes (branches) of your project
// create by Linus Torvalds in 2005 (Linux)

// repo (repository): project folder
// local vs remote

// remote: github/gitlab/bitbucket .... websites to host your repo online

// check version: git --version
// set identity:
// git config --global user.email "gitemail.com"
// git config --global user.name "Your Name"


// to create a new git repo:
// git init
// MAC: brew install git

// git branch

// to add the files to the staging area for tracking
// git add .

// to save the changes
// git commit -m "build a new feature, added padding"

// to create and switch to a branch
// git checkout  -b feature/add-new-line

